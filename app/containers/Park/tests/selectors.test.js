// import { fromJS } from 'immutable';
// import { selectParkDomain } from '../selectors';

describe('selectParkDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
